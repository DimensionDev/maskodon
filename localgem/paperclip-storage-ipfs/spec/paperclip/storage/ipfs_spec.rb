# frozen_string_literal: true

RSpec.describe Paperclip::Storage::Ipfs do
  it "has a version number" do
    expect(Paperclip::Storage::Ipfs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
